Contributor top-tips
A list of tips, tricks and short-cuts that will be useful when adding, updating and correcting data on the site (Most of these have been submitted by the site's most experienced and knowledgeable Contributors)

Tips and Tricks (By Section)
Firefox Add-Ons
Finding Information Quickly
Submission Tips
Get Satisfaction Tips
Firefox Add-Ons
A list of add-ons for the Firefox browser that can speed up the submission process for Contributors.

Fox-Replace

This Firefox add-on allows the bulk replacement of text in web pages (editable text boxes/cells etc). It works brilliantly within the submission system, especially if you want to change or correct dozens of pieces of data/character names. Get it Here

Finding Information Quickly
Reduce the time it takes to find information with this list of time-saving tips.

Precise Text Searches

If the IMDb Advanced Search doesn't satisfy your needs, you can always use Google to get more precise results. For example, if you want to find "Sussex, England, UK" Filming Locations but without "East Sussex, England, UK" and "West Sussex, England, UK", you can use the corresponding include/exclude parameters limiting the results to IMDb Title Filming Locations pages (use the "site" and "inurl" search operators for it): site:www.imdb.com/title inurl:locations "Sussex, England, UK" -"East Sussex" -"West Sussex".


Submitted by: Chicago_gangster

На цьому тижні вам належить розібратися із завданням стиснення даних.

Нагадуємо, що філософія цього етапу: вам дається більше самостійності (немає відео…), збільшується алгоритмічна складність задачок, зате немає нового матеріалу по самій мові.
Далі приведемо декілька рекомендацій, що допоможуть вам уникнути проблем.

Перечитайте кодекс честі в матеріалах курсу, не дивіться чужі рішення ваших завдань (чи значні їх частини)

Зрозумійте нащо вам дані ці завдання. Не робіть “бо сказали”. Наприклад, задачу на калькулятор можна вирішити в декілька строчок, використовуючи вбудовану бібліотеку чи сторонні бібліотеки. Але не робіть так, не для того ви проходите цей курс.
Ваша задача навчитися підходам до комплексного завдання, декомпозувати його, вибудовувати самостійно алгоритм.

Нагадуємо, що списати можна не лише код, але і коментарі, або навіть архітектуру (!) (ви дивитесь чуже рішення, розумієте як робити, і пишете так само і навіть не знатимете що можна зовсім по-іншому). Все це ми вважаємо списуванням і ви знаєте до чого це призводить. Не робіть так.

На цьому курсі вам доведеться гуглити. Не гугліть код. Не читайте чужий код. Можна гуглити описи алгоритму (графічні, текстові, не код). Щодо коду, можна гуглити якусь дрібницю (наприклад “як прочитати текстовий файл” або “як прочитати зображення”). Але ви маєте повністю розуміти код своєї задачі. Якщо ви не розумієте декілька строчок, які використали з інтернету, то вам мають поставити 2. Від цього буде сумно усім.

Якщо вам потрібна допомога і ви не можете самостійно виконати задачу, то, звичайно, користуйтесь допомогою комьюніті в нашому чаті 😉

На вхід програми першим параметром приходить ім’я файлу.

наприклад: test.txt

Якщо параметрів немає, то передбачається що перший параметр все-таки дорівнює test.txt

Другим параметром приходить ім’я файлу, який генерується наприклад test.par (“par” від слів “p2p archiver”).

Якщо другий параметр відсутній, то передбачається що він дорівнює:

<ім'я_вхідного_файлу>.par (у разі якщо архівуємо)
<ім'я_вхідного_файлу_без_.par_в_кінці> (якщо перший параметр закінчується на .par)
<ім'я_вхідного_файлу>.uar (від “unarchived”)
Програма на підставі розширення першого імені файлу обчислює, чи треба їй архівувати або розпакувати.

Також, програма може приймати першим додатковим параметром (зміщуючи два вищеописаних на позиції 2 і 3) прапори:

-a - явна вказівка системі архівувати перший файл у другий
-u - явна вказівка системі розпакувати з першого файлу у другий
Прапори стають у пригоді, якщо ви не хочете з якоїсь причини користуватися розширенням .par, або зробити щось ще більш екзотичне:

Приклади:

виклик_вашої_програми poem.txt
виклик_вашої_програми poem.txt arvhied_poem.par
виклик_вашої_програми poem.txt.par
виклик_вашої_програми poem.txt.par unarvhived_poem.txt
виклик_вашої_програми poem archived
виклик_вашої_програми -u archived unarchived
виклик_вашої_програми -a archive.par archived_twice.par
При успішній архівації програма повинна видати на консоль:

ефективність стиснення / розпакування
час стиснення
розмір вхідного файлу
та файлу, що виходить
Тепер про алгоритм цього тижня:

розробити масив бітових послідовностей, які не перетинаються один з одним і дозволяють
однозначно визначити символ
закодувати файл так, що в ньому на початку буде зберігається таблиця асоціацій символ-бітова послідовність,
а потім вже власне біти стислих даних.
спочатку треба буде вирахувати кількість унікальних символів, а звідси вже кількість біт,
наприклад якщо маємо 12 унікальних символів то маємо xxxx біт, тому що більше 8 але менше 16
нехай у нас файл складається з букв А і В, то за фактом можна взяти всього 1 біт (0 і 1) для кодування кожної літери
якщо у нас три букви А, В, і С, то доведеться взяти 2 біта
** УВАГА **: пропонований алгоритм порядно неоптимальний.
Більш того, він часто може дати результат за розмірами більше, ніж початковий файл.
Є куди більш оптимальні класичні алгоритми - наприклад LZMA, Huffman, DEFLATE …
Але не треба їх імплементити поки що.

Отже, десь глибоко у вашій програмі, виходить, повинні сидіти функції типу: